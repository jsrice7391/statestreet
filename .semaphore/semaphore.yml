version: v1.0
name: Node JS
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Install dependencies
    task:
      jobs:
        - name: yarn install
          commands:
            - sem-version node 8
            - checkout
            - yarn install
            - yarn test
  - name: yarn deploy
    task:
      prologue:
        commands:
          - echo "I did a thing"
      jobs:
        - name: Install
          commands:
            - yarn --version
promotions:
  - name: Deploy Docker
    pipeline_file: pipeline_2.yml
  - name: Deploy S3
    pipeline_file: pipeline_3.yml
